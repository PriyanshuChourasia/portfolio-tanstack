import { createFileRoute } from '@tanstack/react-router'
import { BuilderLayout } from '@/features/resume-builder/components/BuilderLayout'

export const Route = createFileRoute('/')({ component: BuilderLayout })
